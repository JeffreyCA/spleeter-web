{
    "name": "spleeter-web",
    "version": "1.0.0",
    "scripts": {
        "postinstall": "npm install --prefix frontend",
        "heroku-postbuild": "npm run build --prefix frontend",
        "dev": "npm run dev --prefix frontend & python3 manage.py runserver",
        "prod": "npm run prod --prefix frontend & python3 manage.py runserver"
    }
}
