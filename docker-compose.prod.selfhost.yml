version: '3.3'
services:
  nginx:
    volumes:
      - /path/to/media:/webapp/media
  api:
    volumes:
      - /path/to/media:/webapp/media
  celery-fast:
    volumes:
      - /path/to/media:/webapp/media
  celery-slow:
    volumes:
      - /path/to/media:/webapp/media
